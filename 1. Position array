#include <iostream>
using namespace std;

int main() {
    int n, pos, val;
    cin >> n;
    int a[100];
    for(int i=0;i<n;i++) 
    cin >> a[i];
    cin >> pos >> val;

    for(int i=n;i>pos;i--) 
    a[i]=a[i-1];
    a[pos]=val;
    n++;

    for(int i=0;i<n;i++) 
    cout << a[i] << " ";
}
